import it.proactivity.model.PublishingHouse;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

/*public class TestPublishingHouse {
    @Test
    public void createPublishingHouseTest() {
        PublishingHouse adelphi = PublishingHouse.createPublishingHouse("01", "Adelphi", "italian", "Milan", "2380000000f","Luciano Foà",
                "1400");
        assertEquals("Adelphi", adelphi.getName());
        assertEquals("01", adelphi.getId());
        assertEquals("italian", adelphi.getNationality());
        assertEquals("Milan", adelphi.getCity());
        assertEquals("2380000000f", adelphi.getRevenue());
        assertEquals("Luciano Foà", adelphi.getFoundator());
        assertEquals("1400", adelphi.getNumberOfEmployees());
    }

    public void createPublishingHouseTest() {
        PublishingHouse adelphi = PublishingHouse.createPublishingHouse("01", "Adelphi", "italian", "Milan", "2380000000f","Luciano Foà",
                "1400");
        assertNotEquals();
    }
}*/
